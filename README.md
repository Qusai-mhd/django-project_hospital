# django-project_hospital

set up the project:
   create a new virtual env:
       python3 -m venv env
       
   activate the created env:
       source env/bin/activate
       
   install dependencies:
        pip install -r requirements.txt
        
   run the server:
       python manage.py runserver
